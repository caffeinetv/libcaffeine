# libcaffeine Generated Config
# For use with find_package and similar setups. Will fully define and configure everything required.
# 
# Defines Targets:
#   libcaffeine
# 
# Defines Variables:
#   LIBCAFFEINE_INCLUDE_DIR
#   LIBCAFFEINE_LIBRARY_DIR
#   LIBCAFFEINE_BINARY_DIR
#   LIBCAFFEINE_LIBRARY
#   LIBCAFFEINE_BINARY
#   LIBCAFFEINE_BINARY_PDB
#   LIBCAFFEINE_LIBRARY_RELEASE
#   LIBCAFFEINE_BINARY_RELEASE
#   LIBCAFFEINE_LIBRARY_RELWITHDEBINFO
#   LIBCAFFEINE_BINARY_RELWITHDEBINFO
#   LIBCAFFEINE_BINARY_PDB_RELWITHDEBINFO
#   LIBCAFFEINE_LIBRARY_MINSIZEREL
#   LIBCAFFEINE_BINARY_MINSIZEREL
#   LIBCAFFEINE_LIBRARY_DEBUG
#   LIBCAFFEINE_BINARY_DEBUG
#   LIBCAFFEINE_BINARY_PDB_DEBUG
# 

@PACKAGE_INIT@

set_and_check(LIBCAFFEINE_INCLUDE_DIR "@PACKAGE_INCLUDE_INSTALL_DIR@")
set_and_check(LIBCAFFEINE_LIBRARY_DIR "@PACKAGE_LIBRARY_INSTALL_DIR@")
set_and_check(LIBCAFFEINE_BINARY_DIR "@PACKAGE_BINARY_INSTALL_DIR@")
set_and_check(LIBCAFFEINE_LIBRARY "@PACKAGE_LIBRARY_PATH@")
set_and_check(LIBCAFFEINE_BINARY "@PACKAGE_BINARY_PATH@")
if(MSVC)
    set_and_check(LIBCAFFEINE_BINARY_PDB "@PACKAGE_BINARY_PDB_PATH@")
endif()

# Release
set(LIBCAFFEINE_LIBRARY_RELEASE "@PACKAGE_LIBRARY_RELEASE@")
set(LIBCAFFEINE_BINARY_RELEASE "@PACKAGE_BINARY_RELEASE@")

# RelWithDebInfo
set(LIBCAFFEINE_LIBRARY_RELWITHDEBINFO "@PACKAGE_LIBRARY_RELWITHDEBINFO@")
set(LIBCAFFEINE_BINARY_RELWITHDEBINFO "@PACKAGE_BINARY_RELWITHDEBINFO@")
if(MSVC)
    set(LIBCAFFEINE_BINARY_PDB_RELWITHDEBINFO "@PACKAGE_BINARY_PDB_RELWITHDEBINFO@")
endif()

# MinSizeRel
set(LIBCAFFEINE_LIBRARY_MINSIZEREL "@PACKAGE_LIBRARY_MINSIZEREL@")
set(LIBCAFFEINE_BINARY_MINSIZEREL "@PACKAGE_BINARY_MINSIZEREL@")

# Debug
set(LIBCAFFEINE_LIBRARY_DEBUG "@PACKAGE_LIBRARY_DEBUG@")
set(LIBCAFFEINE_BINARY_DEBUG "@PACKAGE_BINARY_DEBUG@")
if(MSVC)
    set(LIBCAFFEINE_BINARY_PDB_DEBUG "@PACKAGE_BINARY_PDB_DEBUG@")
endif()

check_required_components(libcaffeine)

if(NOT TARGET libcaffeine)
    include("${CMAKE_CURRENT_LIST_DIR}/libcaffeineTarget.cmake")
endif()
